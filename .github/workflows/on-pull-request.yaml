name: On Pull Request

on:
  pull_request:

jobs:
  build:
    name: Build
    uses: ./.github/workflows/wf-docker-build.yaml
    with:
      push-ghcr: false
      save-cache: false

  lint:
    name: Lint
    strategy:
      matrix:
        workflow:
          - wf-lint-backend.yaml
    # uses: ./.github/workflows/${{ matrix.workflow }}
    uses: ./.github/workflows/wf-lint-backend.yaml
