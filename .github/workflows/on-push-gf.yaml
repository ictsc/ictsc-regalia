name: On Push God Father

on:
  push:
    branches: [feature/god_father]

jobs:
  build:
    name: Build
    uses: ./.github/workflows/wf-docker-build.yaml
    with:
      push-ghcr: true
      save-cache: false
